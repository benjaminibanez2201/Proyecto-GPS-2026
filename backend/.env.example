HOST=localhost
PORT=3000
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3000
DB_USERNAME=postgres
PASSWORD=tu_password
DATABASE=postgres
DB_PORT=5432
JWT_SECRET=tu_jwt_secret
COOKIE_KEY=tu_cookie_key

# Clave de 32 bytes en hexadecimal (64 caracteres) para cifrar los IDs públicos en las URLs.
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PUBLIC_ID_SECRET=tu_clave_de_64_caracteres_hex

# Nodemailer
EMAIL_USER=email_de_la_app
EMAIL_PASS=app_password_de_gmail
EMAIL_FROM=ArriendU <email_de_la_app>
